# Webサイト公開前チェッカー 実装プラン

TypeScriptを自分で書きながら、Webサイト公開前チェッカーを段階的に作るための計画です。

**進捗：2026年8月28日に全工程完了**

## 進め方

各段階を1つのPull Requestとして進めます。

1. 作業ブランチを作る
2. 自分で実装する
3. ローカルで動作を確認する
4. コードレビューを受ける
5. コミットしてpushする
6. Pull Requestを作成して`main`へマージする

一度に多くの機能を追加せず、各Pull Requestの完了条件を満たしてから次へ進みます。

## コミットメッセージのルール

コミットメッセージは、英語の接頭辞と日本語の説明を組み合わせます。

```text
英語の接頭辞: 日本語の変更内容
```

使用する接頭辞は次のとおりです。

| 接頭辞 | 用途 | 例 |
| --- | --- | --- |
| `feat` | 新しい機能 | `feat: URL入力フォームを追加` |
| `fix` | 不具合の修正 | `fix: 空欄時のエラー表示を修正` |
| `docs` | ドキュメントの変更 | `docs: 実装プランを追加` |
| `refactor` | 動作を変えないコード整理 | `refactor: 診断処理を整理` |
| `test` | テストの追加や修正 | `test: URL検証のテストを追加` |
| `chore` | 設定や開発環境の変更 | `chore: 開発環境の設定を変更` |

説明部分は、変更内容が具体的に分かる短い日本語にします。

## PR 1: URL入力フォーム

ブランチ名：`feature/url-input-form`

### 実装内容

- [x] URLを入力するフォームを作る
- [x] 「チェックする」ボタンを作る
- [x] `submit`イベントを受け取る
- [x] 入力された文字列を取得する

### 学習ポイント

- DOM要素の型指定
- `HTMLFormElement`と`HTMLInputElement`
- イベント処理
- 関数の戻り値に付ける`void`型

### 完了条件

- URLを入力してボタンを押せる
- フォーム送信時にページが再読み込みされない
- 入力値をTypeScriptから取得できる
- `npm run build`が成功する

## PR 2: URLバリデーション

ブランチ名：`feature/url-validation`

### 実装内容

- [x] 空欄を検出する
- [x] URLとして正しい形式か確認する
- [x] `http`または`https`だけを許可する
- [x] エラーメッセージを画面に表示する

### 学習ポイント

- `URL`クラス
- 条件分岐
- `try...catch`
- 関数の引数と戻り値の型

### 完了条件

- 正しいURLを受け付ける
- 不正な入力には適切なメッセージを表示する
- バリデーション処理が表示処理から分離されている

## PR 3: 診断結果の型と仮表示

ブランチ名：`feature/check-result-types`

### 実装内容

- [x] `CheckResult`型を定義する
- [x] 重要度として`pass`、`warning`、`error`を定義する
- [x] 仮の診断結果を配列で用意する
- [x] 診断結果を一覧表示する

### 学習ポイント

- `type`と`interface`
- Union型
- 配列の型
- `map()`を使った表示

### 完了条件

- 型に合わない診断結果をTypeScriptが検出できる
- 3種類の重要度を画面で見分けられる

## PR 4: 基本的なHTML診断

ブランチ名：`feature/basic-html-checks`

### 実装内容

- [x] titleの有無を確認する
- [x] descriptionの有無を確認する
- [x] h1の数を確認する
- [x] 各診断を独立した関数にする

### 学習ポイント

- 純粋関数
- 型を使った診断結果の生成
- 配列操作
- 処理の責務分離

### 完了条件

- 同じ入力から常に同じ診断結果が返る
- DOM表示処理と診断処理が分かれている
- 複数の診断結果をまとめて返せる

## PR 5: HTML取得API

ブランチ名：`feature/site-fetch-api`

### 実装内容

- [x] 対象URLのHTMLを取得するサーバー側APIを作る
- [x] フロントエンドからAPIを呼び出す
- [x] 読み込み中の状態を表示する
- [x] 通信失敗時のエラーを表示する

### 学習ポイント

- `async`と`await`
- `fetch()`
- APIレスポンスの型
- 非同期処理のエラーハンドリング
- ブラウザのCORS制約

### 完了条件

- 入力したURLのHTMLを取得できる
- 通信中と通信完了を画面で判別できる
- タイムアウトや取得失敗を処理できる

## PR 6: 診断項目の追加

ブランチ名：`feature/metadata-checks`

### 実装内容

- [x] OGPを確認する
- [x] canonical URLを確認する
- [x] faviconを確認する
- [x] robots設定を確認する
- [x] 画像のalt属性を確認する

### 学習ポイント

- 複数の診断関数の共通化
- Optionalな値の扱い
- 型ガード
- HTML要素と属性の解析

### 完了条件

- 各項目が`CheckResult`型で返される
- 未設定と設定不備を区別できる
- 新しい診断を追加しやすい構造になっている

## PR 7: 診断結果画面の仕上げ

ブランチ名：`feature/result-summary`

### 実装内容

- [x] 重要度ごとの件数を表示する
- [x] 診断結果を重要度別に表示する
- [x] 再チェックできるようにする
- [x] PCとスマートフォンの表示を整える

### 学習ポイント

- `filter()`と`reduce()`
- UIの状態管理
- 再利用できる表示関数
- アクセシビリティ

### 完了条件

- 問題の有無をひと目で判断できる
- エラーのある項目を優先して確認できる
- キーボードでも操作できる

## PR 8: テストとドキュメント

ブランチ名：`test/site-checker`、`test/check-functions`

### 実装内容

- [x] 診断関数のテストを書く
- [x] 正常・未設定・不正なHTMLをテストする
- [x] エラー処理を見直す
- [x] READMEに使い方と機能をまとめる

### 学習ポイント

- テストしやすい関数設計
- 境界値と異常系
- 保守しやすいドキュメント

### 完了条件

- 主要な診断関数のテストが成功する
- `npm run build`が成功する
- 初めて使う人がREADMEだけで起動できる

## 完成時に確認すること

- [x] URLを入力して診断を開始できる
- [x] 公開前に必要な主要項目を確認できる
- [x] エラー発生時に原因が分かる
- [x] TypeScriptの型が機能ごとに整理されている
- [x] 診断ロジックにテストがある
- [x] READMEに起動方法と使い方が記載されている
